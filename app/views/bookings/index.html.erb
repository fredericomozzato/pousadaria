<h1>Reservas - <%= @inn.name %></h1>

<% if @bookings.empty? %>
  <div>
    <p>Nenhuma reserva encontrada</p>
  </div>
<% end %>

<section id="bookings-list">
  <ul>
    <% @bookings.each do |booking| %>
      <li>
        <%= render booking %>
      </li>
    <% end %>
  </ul>
</section>