<% if @booking.start_date > Date.today %>
  <p>Check-in disponível a partir de <%= l(@booking.start_date) %></p>
<% elsif @booking.confirmed? %>
  <%= button_to "REALIZAR CHECK-IN" %>
<% end %>

<% if Date.today >= @booking.start_date + 2.days %>
  <%= button_to "CANCELAR RESERVA" %>
<% end %>